name: vault
version: 1.0.0
description: Encrypted vault — save assistant messages via /vault, view in dashboard with password
author: Hermes
provides_tools:
  - vault_store
  - vault_list
provides_hooks:
  - post_llm_call
